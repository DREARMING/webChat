extends layout

block content
  table.table.table-striped.table-hover
    thead.thead-dark
      tr
        th 用户名
        th socket信息
    tbody
      for user in userInfo.users
        tr
          td=user.username
          td
            for socket in user.sockets
              span=socket.id + "[" + socket.type + "] "
      else
        tr
          td(colspan="2") 暂无用户信息
  span.float-right 总共#{userInfo.size}个用户